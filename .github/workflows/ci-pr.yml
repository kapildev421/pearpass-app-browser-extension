name: PR CI

on:
  pull_request_target:
    types: [opened, reopened, synchronize]
    branches: [main, dev]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: pr-ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  authorize:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    outputs:
      allowed: ${{ steps.auth.outputs.allowed }}

    steps:
      - name: Checkout workflow code
        uses: actions/checkout@v4

      - name: Authorize PR execution
        id: auth
        uses: ./.github/actions/authorize-pr
        with:
          github-token: ${{ github.token }}

      - name: Summarize skipped run
        if: steps.auth.outputs.allowed != 'true'
        run: echo "PR CI with repository secrets is limited to trusted contributors." >> "$GITHUB_STEP_SUMMARY"

  ci:
    needs: authorize
    if: needs.authorize.outputs.allowed == 'true'
    uses: ./.github/workflows/ci-reusable.yml
    with:
      checkout_ref: ${{ github.event.pull_request.head.sha }}
      checkout_repository: ${{ github.event.pull_request.head.repo.full_name }}
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
